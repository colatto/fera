/// <reference types="vite/client" />

// Versão registrada no package.json do commit construído, injetada pelo
// define do Vite (rodapé institucional — spec interface-web).
declare const __VERSAO_APP__: string
